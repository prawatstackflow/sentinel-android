package com.finvasia.sentinel

/** Color scheme for the verification surface. */
enum class SentinelTheme {
    LIGHT,
    DARK,

    /** Resolve to the device's current light/dark setting at launch time. */
    SYSTEM,
}
